# Core analysis pipeline (analyze / train / predict / explain)
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
nltk>=3.8,<4.0
joblib>=1.3,<2.0

# Charts
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# Tweet collection (only needed for the `scrape` command)
selenium>=4.15,<5.0
webdriver-manager>=4.0,<5.0

# Optional: load credentials from a local .env file
python-dotenv>=1.0,<2.0
